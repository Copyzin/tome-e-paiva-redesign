# dependencies
node_modules/

# build output
dist/
.astro/
.output/

# verification / tooling scratch
.playwright-mcp/

# client deliverables — binary assets kept on disk, not committed
*.pdf

# local scratch — design export prompt, kept on disk but not committed
prompt-claude-code-header-hero.md

# env / secrets
.env
.env.*
!.env.example

# os / editor
.DS_Store
Thumbs.db
*.log
npm-debug.log*
.vscode/*
!.vscode/extensions.json
